VITE_API_URL=http://localhost:5000/api
VITE_STUDENT_PORTAL_URL_BASE=http://localhost:5273

# Public-website's server (NOT its client, which is a different port) —
# admission-application documents (photo, CNIC front/back) are uploaded to
# and served as static files from this origin, not from the main app's own
# server, since the application form lives on public-website. Used by
# CandidateProfilePanel.jsx to build working links to those files.
VITE_PUBLIC_WEBSITE_ORIGIN=http://localhost:5200
